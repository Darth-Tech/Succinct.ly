version: '3'
services:
    application:

        image: devagastya0/text_summary:stable
        ports:
          - "5000:5000"
        volumes: 
          - ./:/app
        command: ["python","/app/api.py"]
        restart: always
